/** Types of chart-update, in pipeline execution order. */
export declare enum ChartUpdateType {
    FULL = 0,
    UPDATE_DATA = 1,
    PROCESS_DATA = 2,
    PROCESS_DOMAIN = 3,
    PROCESS_RANGE = 4,
    PERFORM_LAYOUT = 5,
    PRE_SERIES_UPDATE = 6,
    SERIES_UPDATE = 7,
    PRE_SCENE_RENDER = 8,
    SCENE_RENDER = 9,
    NONE = 10
}
