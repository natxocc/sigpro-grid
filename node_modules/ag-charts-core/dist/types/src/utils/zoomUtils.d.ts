export interface ZoomMinMax {
    min: number;
    max: number;
}
export interface ZoomMinMaxDirection extends ZoomMinMax {
    direction: 'x' | 'y';
}
export interface DefinedZoomState {
    x: ZoomMinMax;
    y: ZoomMinMax;
}
export interface ZoomState {
    x?: ZoomMinMax;
    y?: ZoomMinMax;
    autoScaleYAxis?: boolean;
}
export declare const UNIT_MIN = 0;
export declare const UNIT_MAX = 1;
export declare function definedZoomState(zoom?: ZoomState): DefinedZoomState;
