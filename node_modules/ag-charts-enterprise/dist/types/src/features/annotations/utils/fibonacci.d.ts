import type { Bounds4 } from 'ag-charts-core';
import type { AnnotationContext, FibonacciBands } from '../annotationTypes';
export declare enum FibonacciNodeTag {
    OneLine = 0,
    HorizontalLine = 1
}
type FibonacciRangeLabel = Bounds4 & {
    text: string;
};
export interface FibonacciRangeDatum extends Bounds4 {
    id: any;
    tag: FibonacciNodeTag;
    label: FibonacciRangeLabel;
}
export declare function getFibonacciCoords(coords1: Bounds4, coords2?: Bounds4): {
    x1: number;
    x2: number;
    y1: number;
    y2: number;
};
export declare function createFibonacciRangesData({ x1, y1, x2, y2 }: Bounds4, context: AnnotationContext, reverse: boolean, yZero: number, bands?: FibonacciBands): FibonacciRangeDatum[];
export {};
