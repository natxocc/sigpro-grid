import type { Point } from 'ag-charts-core';
import type { PointProperties } from '../annotationProperties';
import type { AnnotationAxisContext, AnnotationContext, DataPoint } from '../annotationTypes';
export declare function convertLine(datum: {
    start: Pick<PointProperties, 'x' | 'y'>;
    end: Pick<PointProperties, 'x' | 'y'>;
}, context: AnnotationContext): {
    x1: number;
    y1: number;
    x2: number;
    y2: number;
} | undefined;
export declare function convertPoint(point: DataPoint, context: AnnotationContext): {
    x: number;
    y: number;
};
export declare function convert(p: DataPoint['x' | 'y'], context: Pick<AnnotationAxisContext, 'scale' | 'snapToGroup'>): number;
export declare function invertCoords(coords: Point, context: AnnotationContext): {
    x: any;
    y: any;
};
export declare function invert(n: number, context: Pick<AnnotationAxisContext, 'scale' | 'continuous' | 'scaleInvert' | 'scaleInvertNearest'>): any;
