import { _ModuleSupport } from 'ag-charts-community';
import { AbstractModuleInstance } from 'ag-charts-core';
import { HorizontalScrollbarProperties, ScrollbarThumb, ScrollbarTrack, VerticalScrollbarProperties } from './scrollbarProperties';
export declare class Scrollbar extends AbstractModuleInstance {
    private readonly ctx;
    enabled?: boolean;
    thickness?: number;
    spacing?: number;
    tickSpacing?: number;
    placement?: 'inner' | 'outer';
    visible?: 'auto' | 'always' | 'never';
    track: ScrollbarTrack;
    thumb: ScrollbarThumb;
    horizontal: HorizontalScrollbarProperties;
    vertical: VerticalScrollbarProperties;
    private readonly state;
    private seriesRect?;
    constructor(ctx: _ModuleSupport.ModuleContext);
    private createOrientationState;
    private resolveProperties;
    private getDefaultPosition;
    private resolveAxis;
    private onLayoutStart;
    private onLayoutComplete;
    private getLayoutRect;
    private updateStyles;
    private updateTrack;
    private updateThumb;
    private updateThumbs;
    private updateVisibility;
    private getZoomRange;
    private handleUserChange;
    private handleHoverChange;
    destroy(): void;
}
