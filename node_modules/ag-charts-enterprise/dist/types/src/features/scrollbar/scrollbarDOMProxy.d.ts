import { _ModuleSupport } from 'ag-charts-community';
import type { BoxBounds } from 'ag-charts-core';
type ScrollbarDOMProxyCtx = Pick<_ModuleSupport.ModuleContext, 'proxyInteractionService' | 'localeManager'>;
export declare class ScrollbarDOMProxy {
    private readonly ctx;
    private readonly orientation;
    private readonly onChange;
    private readonly onHoverChange;
    private readonly container;
    private readonly slider;
    private readonly thumbFocus;
    private dragStartRatio;
    private interactionMode;
    private interactionBounds?;
    private readonly state;
    private readonly repeater;
    constructor(ctx: ScrollbarDOMProxyCtx, orientation: 'horizontal' | 'vertical', onChange: (min: number, max: number) => void, onHoverChange: (hovered: boolean) => void);
    destroy(): void;
    updateBounds(bounds: BoxBounds): void;
    updateVisibility(visible: boolean): void;
    updateMinMax(min: number, max: number, thumbSpan?: number, options?: {
        skipSliderUpdate?: boolean;
    }): void;
    updateThumbBounds(thumb: BoxBounds, track: BoxBounds, cornerRadius?: number): void;
    private update;
    private onSliderChange;
    private onSliderKeyDown;
    private onDragMove;
    private onDragEnd;
    private onDragStart;
    private onMouseLeave;
    private getClickInfo;
    private getPointerRatio;
    private getPointerInfo;
    private jumpTo;
    private applyStepToward;
    private beginStepRepeat;
    private setInteraction;
    private getInteractionBounds;
    private isWithinThumb;
    private handleHoverEvent;
}
export {};
