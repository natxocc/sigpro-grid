import type { AgZoomAutoScaling } from 'ag-charts-community';
import { _ModuleSupport } from 'ag-charts-community';
import type { DeepRequired } from 'ag-charts-core';
import { BaseProperties, ChartAxisDirection, CleanupRegistry } from 'ag-charts-core';
type ZoomAutoScalingOpts = DeepRequired<AgZoomAutoScaling>;
export declare class ZoomAutoScalingProperties extends BaseProperties implements ZoomAutoScalingOpts {
    constructor();
    enabled: boolean;
    padding: number;
}
interface ZoomAutoScalerPropertiesDeps {
    readonly enabled: boolean;
    readonly enableIndependentAxes?: boolean;
}
export declare class ZoomAutoScaler {
    private readonly properties;
    private readonly zoomManager;
    private readonly deps;
    constructor(properties: ZoomAutoScalingProperties, zoomManager: _ModuleSupport.ZoomManager, deps: ZoomAutoScalerPropertiesDeps, eventsHub: _ModuleSupport.EventsHub, eventsCleanup: CleanupRegistry);
    private manuallyAdjusted;
    get enabled(): boolean;
    onManualAdjustment(direction: ChartAxisDirection): void;
    private onChangeRequest;
    private hasYAxisChange;
    private onSaveMemento;
    private onLoadMemento;
    private getAutoScaleYZoom;
    private autoScaleYZoom;
    private zoomBounds;
    private primaryAxisZoom;
    private combinedAxisZoom;
}
export {};
