import { _ModuleSupport } from 'ag-charts-community';
import type { CleanupRegistry, DeepRequired } from 'ag-charts-core';
import { BaseProperties } from 'ag-charts-core';
import type { AgZoomOnDataChange, AgZoomOnDataChangeStrategy } from 'ag-charts-types';
type ModuleContext = Pick<_ModuleSupport.ModuleContext, 'eventsHub' | 'zoomManager' | 'axisManager'>;
export declare class ZoomOnDataChangeProperties extends BaseProperties implements DeepRequired<AgZoomOnDataChange> {
    strategy: AgZoomOnDataChangeStrategy;
    stickToEnd: boolean;
}
export declare class ZoomOnDataChange {
    private readonly onConstrainChangesCallback;
    private readonly properties;
    private readonly ctx;
    private desiredChanges?;
    constructor(onConstrainChangesCallback: (e: _ModuleSupport.ZoomChangeRequestEvent) => void, properties: ZoomOnDataChangeProperties, ctx: ModuleContext, eventsCleanup: CleanupRegistry);
    destroy(): void;
    private onDataLoad;
    private onDataUpdate;
    private onZoomChangeRequest;
    /**
     * Convert ambiguous axes-scale (number | Date | string) into a strictly numerical scale, so that we can use
     * interpolation to implement preserveDomain in an axes-scale agnostic way.
     */
    private computeDomainMinMax;
    private popDesiredChanges;
    private performUpdateStrategy;
    private performPreserveDomain;
    private performStickToEnd;
}
export {};
