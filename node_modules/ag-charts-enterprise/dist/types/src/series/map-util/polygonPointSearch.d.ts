import type { Position } from 'ag-charts-core';
export declare function polygonPointSearch(polygons: Position[][], precision: number, valueFn: (polygons: Position[][], x: number, y: number, stride: number) => {
    distance: number;
    maxDistance: number;
}): {
    x: number;
    y: number;
    distance: number;
} | undefined;
