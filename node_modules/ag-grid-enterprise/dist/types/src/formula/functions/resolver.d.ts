import type { AgColumn, BeanCollection, RowNode } from 'ag-grid-community';
import type { FormulaNode } from '../ast/utils';
import type { CellFormula } from '../formulaService';
export declare function evalAst(beans: BeanCollection, node: FormulaNode, getCellValue: (addr: {
    row: RowNode;
    column: AgColumn;
}) => unknown, caller: {
    row: RowNode;
    column: AgColumn;
}): unknown;
export type Addr = {
    row: RowNode;
    column: AgColumn;
};
/**
 * Streams uncached formula dependencies from an AST in traversal order.
 * Skips primitives, non-formula cells, cached formula cells, and already-done cells.
 */
export declare function unresolvedDeps(beans: BeanCollection, root: FormulaNode, ensureFormulaCache: (row: RowNode, col: AgColumn) => CellFormula | null): Generator<Addr>;
