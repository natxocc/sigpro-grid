{
  "name": "sigpro-grid",
  "version": "35.2.0",
  "type": "module",
  "main": "./grid/index.js",
  "module": "./grid/index.js",
  "types": "./index.d.ts",
  "files": [
    "grid"
  ],
  "scripts": {
    "build": "bun build main.js --outfile grid/grid.js --minify --format esm --target node && node -e \"require('fs').writeFileSync('grid/grid.js', require('fs').readFileSync('grid/grid.js', 'utf8').replace(/\\n/g,' '))\""
  },
  "devDependencies": {},
  "dependencies": {
    "ag-grid-community": "^35.2.0",
    "ag-grid-enterprise": "^35.2.0"
  }
}
